spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://c://Users/wii64/git/microservices-ecommerce/config-data
          clone-on-start: true
          default-label: main
server:
  port: 8888