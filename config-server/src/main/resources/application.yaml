spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
#          uri: file://c://Users/wii64/git/microservices-ecommerce/config-data
          uri: https://github.com/WiiruxCruz/microservices-config-data
          username: ${GITHUB_USER}
          password: ${GITHUB_TOKEN}
          clone-on-start: true
          default-label: main
server:
  port: 8888